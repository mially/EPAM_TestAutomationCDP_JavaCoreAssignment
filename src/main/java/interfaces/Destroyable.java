package com.epam.cdpWeek2project.interfaces;

/** Destroyable interface can be applied to all destroyable objects - toys or playrooms */
public interface Destroyable {
    /**destroy() object and change state and fields accordingly*/
    void destroy();
}
